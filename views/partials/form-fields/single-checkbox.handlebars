<div class="row">
    <div class="col s12">
        <input name="{{fields.id}}" type="checkbox" id="{{fields.id}}" {{#if default_value}}checked{{/if}} />
        <label for="{{fields.id}}">{{label_text}}</label>
    </div>
</div>